{
  "name": "MATERIA dev container",
  "dockerFile": "../Dockerfile.dev",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
  ],
  "workspaceFolder": "/workspace",
  "forwardPorts": [5173],
  "postCreateCommand": "pnpm install",
  "remoteUser": "root"
}
